{
  "buildCommand": "cd .. && pwd && ls -la && cd web && npm install && npm run build",
  "outputDirectory": "web/dist",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
