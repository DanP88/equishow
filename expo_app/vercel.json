{
  "buildCommand": "npm install && npx expo export --platform web && node scripts/inject-pwa-meta.mjs",
  "outputDirectory": "dist",
  "installCommand": "echo skip install",

  "rewrites": [
    {
      "source": "/((?!.*\\.).*)",
      "destination": "/index.html"
    }
  ]
}